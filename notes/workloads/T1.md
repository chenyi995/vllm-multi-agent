# T1 · Sequential / Pipeline（顺序流水线）

> 术语（token、层、K/V、block、前缀共享、RoPE、τ、采样层、workflow、容量档）
> 一律见 [`README.md`](README.md) 的公共背景，本文不重复定义。
> 代码：`kvpim/workloads/t1_sequential.py`

## 一句话

**五个角色排成一条流水线，依次加工同一个编程任务，后面的人能看到前面所有人的产出。**

---

## 1. 模拟的是什么

来自 **MetaGPT**（Hong et al., ICLR'24）。这篇论文的想法是：让大模型模仿一家软件公司，
五个角色**顺序**处理同一个任务，每人只做自己那一档，把产出交给下一个人。

我们照搬它的五个角色。**每个角色的系统提示词逐字节取自 MetaGPT 源码**
（`ref/MetaGPT/metagpt/roles/*.py`），由该仓库自己的模板拼成：

```
"You are a {profile}, named {name}, your goal is {goal}. the constraint is {constraints}. "
```

| 角色 | 名字 | 职责（goal，原文） |
|---|---|---|
| Product Manager | Alice | 写产品需求文档 |
| Architect | Bob | 设计简洁、可用、完整的系统，输出系统设计 |
| Project Manager | Eve | 按需求/设计拆任务清单，分析依赖 |
| Engineer | Alex | 写优雅、可读、可扩展、高效的代码 |
| QaEngineer | Edward | 写全面稳健的测试 |

拼出来的实际系统提示词（engineer 那条）：

```
You are a Engineer, named Alex, your goal is write elegant, readable, extensible,
efficient code. the constraint is the code should conform to standards like
google-style and be modular and maintainable. Use same language as user requirement. 
```

有一条自动校验（每组配置跑完自动执行）：从 `ref/MetaGPT` 重新解析模板与角色字段、
重建系统提示词、与代码里的比对，**五个角色必须逐字节相等**，改一个词就报错。

---

## 2. 具体任务

**HumanEval** —— OpenAI 的 164 道 Python 编程题。每题给一个函数签名加文档字符串，
要求补全实现。我们按难度（标准答案的行数）分层抽 10 道，固定随机种子：

| 题号 | 函数 | 要求 |
|---|---|---|
| HumanEval/40 | `triples_sum_to_zero` | 整数列表里是否存在三个不同元素之和为 0 |
| HumanEval/41 | `car_race_collision` | 两队车对撞，数碰撞次数 |
| HumanEval/49 | `modp` | 算 2^n mod p |
| HumanEval/82 | `prime_length` | 字符串长度是不是质数 |
| HumanEval/105 | `by_length` | 数组排序后按长度替换成英文单词 |
| HumanEval/116 | `sort_array` | 按二进制里 1 的个数排序 |
| HumanEval/117 | `select_words` | 选出含 n 个辅音的单词 |
| HumanEval/119 | `match_parens` | 两个括号串能否拼成合法串 |
| HumanEval/151 | `double_the_difference` | 奇数的平方和 |
| HumanEval/155 | `even_odd_count` | 数一个整数里奇数字和偶数字各几个 |

一道题 = 一个 workflow。

---

## 3. 一道题实际是怎么跑的

以 `HumanEval/40` 为例，**下面是真实 trace 里的数字**：

```
call  角色                输入长度   输出长度   缓存命中
 0    product_manager        217       367        0
 1    architect              599       340        0
 2    project_manager        940       340        0
 3    engineer              1283       373        0
 4    qa_engineer           1702       297        0
```

### 特征一：输入单调增长

`217 → 599 → 940 → 1283 → 1702`。因为每个角色都要看到前面所有人的产出——
第 5 个角色的输入里包含了原始题目 + 前四个角色写的全部东西。

### 特征二：缓存命中全是 0

后一次的输入明明**完整包含**了前一次的内容，为什么一次都没命中？

因为**每一步的系统提示词都换人了**（Alice → Bob → Eve → Alex → Edward），
而系统提示词排在最前面：

```
call 0:  [Alice 的提示词][题目]
call 1:  [Bob   的提示词][题目][Alice 的产出]
          ^^^^^^^^^^^^^^ 第 0 个 token 就不同了 → 后面全部作废
```

前缀共享要求"从头开始连续相同"，所以这里一个 block 都共享不了。

> **这就是问题所在：内容明明大量重复，缓存却一点用不上。**

### 特征三：QA 这一步是真的在执行代码

Engineer 写完实现后，QaEngineer 写出 `assert` 测试，我们**真的起一个子进程去跑**，
不是让模型自己判断对错。失败的话，真实的 `Traceback` 被交回给 Engineer 重写，
最多重试 3 次。

隔离措施：独立子进程、临时目录、10 秒墙钟上限、2 GB 内存上限、最小环境变量。
实测能正确拦下死循环（10 秒被杀）和内存炸弹（MemoryError）。
⚠️ 但这是**资源沙箱不是安全沙箱**：生成的代码仍能读写文件、起子进程、联网。

十道题的实际结果：

```
每题调用数 = {5, 5, 11, 11, 9, 11, 11, 11, 7, 5}     共 86 次调用
QA 判定 28 次 → 触发 Engineer 重试 18 次
→ 3 道题一次通过（5 次调用），6 道题重试到上限（11 次调用）
```

---

## 4. 量出来了什么

以下是 `block 档 = 16、充足档` 那一组的结果。

### 4.1 漏斗

```
n_total            = 5,459 个 block 存进过缓存
n_blocks_in_groups = 2,872 个（52.6%）有内容完全相同的孪生兄弟   ← 理论天花板
```

### 4.2 重复是几个一组

```
 1 个块相同 : 2,587 组 →  2,587 块   ← 内容独一无二，不是候选
 2 个块相同 :   808 组 →  1,616 块   ← 主体（占候选的 56%）
 3 个块相同 :   224 组 →    672 块
 4 个块相同 :    64 组 →    256 块
 5 个块相同 :    25 组 →    125 块
 6 / 7 个   :  各 1 组 →     13 块
10 个块相同 :    19 组 →    190 块   ← 恰好等于题目数
                        候选合计 2,872 块
```

**以两两相同为主，但有一条清晰的长尾。**

### 4.3 重复从哪来

| 来源 | 块数 | 是什么 |
|---|---|---|
| **同一道题内部** | 2,627（91.5%） | 同一段文字在不同角色的输入里落在不同位置 |
| **跨题目** | 245（8.5%） | 五个角色的系统提示词，每道题都出现一次 |

那 19 个"十连组"解码出来就是角色提示词本身：

```
位置 0  : '<|im_start|>system\nYou are a Product Manager, named Alice, your goal is Create'
位置 16 : ' a Product Requirement Document or market research/competitive product research.. the constraint is utilize'
位置 32 : ' the same language as the user requirements for seamless communication. <|im_end|>\n<|im_start|>user'
```

十道题开头一模一样，因为 workflow 之间清空缓存，所以各存了一份，共 10 份。

而占 91.5% 的同题内重复长这样：

```
同一道题（w00），同样的内容，出现在两个不同位置：位置 64 和位置 608
内容: ': list):\n    """\n    triples_sum_to_zero takes a list of integers as'
```

**这就是位置错配的机制**：同一段函数签名，在 Product Manager 的输入里落在位置 64；
在 Architect 的输入里，因为前面塞进了 PM 的产出，挪到了位置 608。
内容一字不差，但 RoPE 按位置旋转的角度差了一大截，K 的比特完全不同。

### 4.4 三个视角逐层的结果

单位是"块数"，总候选 2,872 块：

| 视角 | 第 0 层 | 第 8 层 | 第 17 层 | 第 26 层 | 第 35 层 | 5 层全判同 |
|---|---|---|---|---|---|---|
| `K_post`（缓存现状） | 202 | 200 | 200 | 200 | 200 | 200 |
| `K_derope`（去掉位置） | **2,872** | 200 | 371 | 526 | 405 | 200 |
| `V`（不带位置的对照） | **2,872** | 167 | 254 | 314 | 222 | 167 |

**第 0 层**：去掉位置之后 2,872 个候选**全部**判同，从 202 涨到 2,872，**14 倍**。
因为第 0 层的 K 只取决于"这是哪个词 + 它在第几位"，位置一拿掉就只剩"这是哪个词"。

**第 8 层**：200 → 200，**一点收益都没有**。

**为什么越深收益越小**：深层的 K 是"看过前面所有 token"之后的产物。两段文字即使
一字不差，只要它们**前面的上下文不同**，深层算出来的 K 就是**真的不同**——这个差异
不是位置造成的，把旋转转回去也救不回来。

> ## 结论
> **位置不是唯一的障碍，上下文才是。**
> 位置无关存储（存不带旋转的 K、读的时候再加上）能拿到的收益**集中在浅层**。
> 如果硬件设计假设"去掉 RoPE 就能全层去重"，这批数据不支持那个假设。

---

## 5. 怎么自己跑一遍

```bash
cd /home/cw636/chenyi/KVPIM/scratch
sbatch -p athena-mini -w node1 --job-name=T1-b16-ample \
  --export=ALL,TOPOLOGY=T1,BLOCK_TIER=16,CAPACITY=ample,NUM_TASKS=10,ZMQ_PORT=5831 \
  run_config.sbatch
```

产出在 `/home/cw636/chenyi/KVPIM/traces/T1_b16_ample/`，该目录的 `README.md`
自动记录了这一组的全部参数与结果。

受限档（`CAPACITY=limited`）**必须在充足档之后跑**——它启动时会读充足档的
`manifest.json` 自算显存池大小。

---

## 6. 这组数字的适用边界

1. **τ 是工作值**。第 26 层的 τ 是 0.964，比第 0 层的 0.99999 松三个数量级——
   那一层的 526 里有多少是阈值太松造成的，重标之前不能下结论。
2. **`max_tokens=1024`**（MetaGPT 原文不限制）。压短输出会让固定长度的角色提示词
   在输入里占比上升，**可能人为抬高重复率**。
3. **与真实 MetaGPT 的一处偏差**：MetaGPT 通过 Action 类产出**结构化文档**
   （PRD、带 mermaid 图的系统设计、JSON 任务清单），我们是把累积的上下文直接喂给角色，
   产出是自由格式文本。**实际上下文长度会比真实 MetaGPT 短一个量级。**
4. 只有一组配置（block 档 16、充足档）的完整计数；其余档位仍在跑。
